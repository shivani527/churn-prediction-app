Put your trained artifacts here, generated by the notebook:

    churn_model.pkl
    selected_cols.pkl

(These are exactly the two files the notebook saves with:
    joblib.dump(best_model_obj, "churn_model.pkl")
    joblib.dump(list(X_train.columns), "selected_cols.pkl")
)

Until both files exist, the API automatically serves predictions from a
transparent rule-based fallback and marks every response with
"demo_mode": true, so the app is fully runnable without them.
